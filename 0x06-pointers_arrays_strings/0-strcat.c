#include "main.h"

/**
 * _strcat - concatenate two strings
 * @dest: parameter 1
 * @src: parameter 2
 * Return: dest
 */

char *_strcat(char *dest, char *src);
{
int dest = i;
int src = j;

i = 0;
while (dest[i] != '\0'; i++)
	dest++;
{
j = 0;
while (src[j] != '\0'; i++)
	src++;
}
dest[i] = src[j]
i++;
}

